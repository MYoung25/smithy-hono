{
  "//": "Type-checks the Worker entry. esbuild (the deploy smoke + wrangler) STRIPS types without checking them, so this is the only real type gate on src/worker.ts. ARCH-01: types:[] (no @types/node) + lib DOM keeps the entry web-standard-only — a node:* / Node-global slip is a type error here.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM"],
    "types": [],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src"]
}
