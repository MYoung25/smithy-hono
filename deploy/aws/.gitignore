# CDK synth output — regenerated by `cdk synth`/`cdk deploy`.
cdk.out/
# Lambda bundling scratch.
.cdk.staging/
# Installed deps (also covered by the root node_modules rule).
node_modules/
# Lockfile is left to the operator's install (file: local deps resolve fresh).
package-lock.json
