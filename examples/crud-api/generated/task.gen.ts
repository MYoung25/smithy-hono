// DO NOT EDIT — regenerated by smithy-hono on every build

import { Hono } from 'hono'
import type { Context, MiddlewareHandler } from 'hono'
import { zValidator } from '@hono/zod-validator'
import { z } from 'zod'
import type { SecurityEnv } from '@smithy-hono/security-core'

// ---- Schemas ----

export const TaskBodySchema = z.object({
  title: z.string(),
  done: z.boolean().optional(),
}).strict()
export type TaskBody = z.infer<typeof TaskBodySchema>

export const CreateTaskInputSchema = z.object({
  body: TaskBodySchema,
}).strict()
export type CreateTaskInput = z.infer<typeof CreateTaskInputSchema>

export const TaskDataSchema = z.object({
  id: z.string(),
  title: z.string(),
  done: z.boolean().optional(),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
}).strict()
export type TaskData = z.infer<typeof TaskDataSchema>

export const CreateTaskOutputSchema = z.object({
  item: TaskDataSchema,
}).strict()
export type CreateTaskOutput = z.infer<typeof CreateTaskOutputSchema>

export const DeleteTaskInputSchema = z.object({
  id: z.string(),
}).strict()
export type DeleteTaskInput = z.infer<typeof DeleteTaskInputSchema>

export const DeleteTaskOutputSchema = z.object({
}).strict()
export type DeleteTaskOutput = z.infer<typeof DeleteTaskOutputSchema>

export const GetTaskInputSchema = z.object({
  id: z.string(),
}).strict()
export type GetTaskInput = z.infer<typeof GetTaskInputSchema>

export const GetTaskOutputSchema = z.object({
  item: TaskDataSchema,
}).strict()
export type GetTaskOutput = z.infer<typeof GetTaskOutputSchema>

export const ListTasksInputSchema = z.object({
  nextToken: z.string().optional(),
  maxResults: z.number().int().min(1).max(100).optional(),
}).strict()
export type ListTasksInput = z.infer<typeof ListTasksInputSchema>

export const ListTasksOutputSchema = z.object({
  items: z.array(TaskDataSchema),
  nextToken: z.string().optional(),
}).strict()
export type ListTasksOutput = z.infer<typeof ListTasksOutputSchema>

export const UpdateTaskInputSchema = z.object({
  id: z.string(),
  body: TaskBodySchema,
}).strict()
export type UpdateTaskInput = z.infer<typeof UpdateTaskInputSchema>

export const UpdateTaskOutputSchema = z.object({
  item: TaskDataSchema,
}).strict()
export type UpdateTaskOutput = z.infer<typeof UpdateTaskOutputSchema>

// ---- Errors ----

export class ValidationError extends Error {
  readonly $statusCode = 400
  readonly $fault = 'client' as const
  constructor(message: string) {
    super(message)
    this.name = 'ValidationError'
    Object.setPrototypeOf(this, ValidationError.prototype)
  }
}

export class TaskNotFound extends Error {
  readonly $statusCode = 404
  readonly $fault = 'client' as const
  constructor(message: string) {
    super(message)
    this.name = 'TaskNotFound'
    Object.setPrototypeOf(this, TaskNotFound.prototype)
  }
}

// ---- Operations interface ----

export interface TaskOperations {
  CreateTask(input: { body: TaskBody }, c?: Context<SecurityEnv>): Promise<CreateTaskOutput>
  DeleteTask(input: { id: string }, c?: Context<SecurityEnv>): Promise<void>
  GetTask(input: { id: string }, c?: Context<SecurityEnv>): Promise<GetTaskOutput>
  ListTasks(input: { nextToken?: string; maxResults?: number }, c?: Context<SecurityEnv>): Promise<ListTasksOutput>
  UpdateTask(input: { id: string; body: TaskBody }, c?: Context<SecurityEnv>): Promise<UpdateTaskOutput>
}

// ---- Middleware interface ----

export interface TaskMiddleware {
  all?: MiddlewareHandler[]
  CreateTask?: MiddlewareHandler[]
  DeleteTask?: MiddlewareHandler[]
  GetTask?: MiddlewareHandler[]
  ListTasks?: MiddlewareHandler[]
  UpdateTask?: MiddlewareHandler[]
}

// ---- Router factory ----

function _chain(mws: MiddlewareHandler[]): MiddlewareHandler {
  return async (c, next) => {
    let i = 0
    const run = async (): Promise<void> => { i < mws.length ? await mws[i++](c, run) : await next() }
    await run()
  }
}

export function createTaskRouter(ops: TaskOperations, middleware?: TaskMiddleware): Hono {
  const app = new Hono()

  // @http(method: "POST", uri: "/tasks")
  app.post('/tasks',
    _chain([...(middleware?.all ?? []), ...(middleware?.CreateTask ?? [])]),
    zValidator('json', TaskBodySchema, (result, c) => {
      if (!result.success)
        return c.json({ code: 'ValidationException', fieldErrors: result.error.issues.map(i => ({ path: i.path.join('.'), code: i.code })) }, 400)
    }),
    async (c) => {
      const input = { body: c.req.valid('json') }
      try {
        const result = await ops.CreateTask(input, c as unknown as Context<SecurityEnv>)
        return c.json(result, 201)
      } catch (e) {
        if (e instanceof ValidationError)
          return c.json({ code: 'ValidationError', message: e.message }, e.$statusCode)
        return c.json({ code: 'InternalServerError', message: 'Internal server error' }, 500)
      }
    }
  )

  // @http(method: "DELETE", uri: "/tasks/{id}")
  app.delete('/tasks/:id',
    _chain([...(middleware?.all ?? []), ...(middleware?.DeleteTask ?? [])]),
    zValidator('param', z.object({
      id: z.string(),
    }), (result, c) => {
      if (!result.success)
        return c.json({ code: 'ValidationException', fieldErrors: result.error.issues.map(i => ({ path: i.path.join('.'), code: i.code })) }, 400)
    }),
    async (c) => {
      const input = { ...c.req.valid('param') }
      try {
        await ops.DeleteTask(input, c as unknown as Context<SecurityEnv>)
        return c.body(null, 204)
      } catch (e) {
        if (e instanceof ValidationError)
          return c.json({ code: 'ValidationError', message: e.message }, e.$statusCode)
        if (e instanceof TaskNotFound)
          return c.json({ code: 'TaskNotFound', message: e.message }, e.$statusCode)
        return c.json({ code: 'InternalServerError', message: 'Internal server error' }, 500)
      }
    }
  )

  // @http(method: "GET", uri: "/tasks/{id}")
  app.get('/tasks/:id',
    _chain([...(middleware?.all ?? []), ...(middleware?.GetTask ?? [])]),
    zValidator('param', z.object({
      id: z.string(),
    }), (result, c) => {
      if (!result.success)
        return c.json({ code: 'ValidationException', fieldErrors: result.error.issues.map(i => ({ path: i.path.join('.'), code: i.code })) }, 400)
    }),
    async (c) => {
      const input = { ...c.req.valid('param') }
      try {
        const result = await ops.GetTask(input, c as unknown as Context<SecurityEnv>)
        return c.json(result, 200)
      } catch (e) {
        if (e instanceof ValidationError)
          return c.json({ code: 'ValidationError', message: e.message }, e.$statusCode)
        if (e instanceof TaskNotFound)
          return c.json({ code: 'TaskNotFound', message: e.message }, e.$statusCode)
        return c.json({ code: 'InternalServerError', message: 'Internal server error' }, 500)
      }
    }
  )

  // @http(method: "GET", uri: "/tasks")
  app.get('/tasks',
    _chain([...(middleware?.all ?? []), ...(middleware?.ListTasks ?? [])]),
    zValidator('query', z.object({
      nextToken: z.string().optional(),
      maxResults: z.string().regex(/^-?\d+$/).transform(Number).pipe(z.number().int().min(1).max(100)).optional(),
    }), (result, c) => {
      if (!result.success)
        return c.json({ code: 'ValidationException', fieldErrors: result.error.issues.map(i => ({ path: i.path.join('.'), code: i.code })) }, 400)
    }),
    async (c) => {
      const input = { ...c.req.valid('query') }
      try {
        const result = await ops.ListTasks(input, c as unknown as Context<SecurityEnv>)
        return c.json(result, 200)
      } catch (e) {
        if (e instanceof ValidationError)
          return c.json({ code: 'ValidationError', message: e.message }, e.$statusCode)
        return c.json({ code: 'InternalServerError', message: 'Internal server error' }, 500)
      }
    }
  )

  // @http(method: "PUT", uri: "/tasks/{id}")
  app.put('/tasks/:id',
    _chain([...(middleware?.all ?? []), ...(middleware?.UpdateTask ?? [])]),
    zValidator('param', z.object({
      id: z.string(),
    }), (result, c) => {
      if (!result.success)
        return c.json({ code: 'ValidationException', fieldErrors: result.error.issues.map(i => ({ path: i.path.join('.'), code: i.code })) }, 400)
    }),
    zValidator('json', TaskBodySchema, (result, c) => {
      if (!result.success)
        return c.json({ code: 'ValidationException', fieldErrors: result.error.issues.map(i => ({ path: i.path.join('.'), code: i.code })) }, 400)
    }),
    async (c) => {
      const input = {
        ...c.req.valid('param'),
        body: c.req.valid('json')
      }
      try {
        const result = await ops.UpdateTask(input, c as unknown as Context<SecurityEnv>)
        return c.json(result, 200)
      } catch (e) {
        if (e instanceof ValidationError)
          return c.json({ code: 'ValidationError', message: e.message }, e.$statusCode)
        if (e instanceof TaskNotFound)
          return c.json({ code: 'TaskNotFound', message: e.message }, e.$statusCode)
        return c.json({ code: 'InternalServerError', message: 'Internal server error' }, 500)
      }
    }
  )

  return app
}
